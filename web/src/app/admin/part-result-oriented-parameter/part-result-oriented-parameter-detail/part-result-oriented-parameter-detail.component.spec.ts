/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PartResultOrientedParameterDetailComponent } from './part-result-oriented-parameter-detail.component';

describe('PartResultOrientedParameterDetailComponent', () => {
  let component: PartResultOrientedParameterDetailComponent;
  let fixture: ComponentFixture<PartResultOrientedParameterDetailComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PartResultOrientedParameterDetailComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PartResultOrientedParameterDetailComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
