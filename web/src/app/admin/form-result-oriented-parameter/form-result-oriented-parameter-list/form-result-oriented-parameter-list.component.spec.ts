/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FormResultOrientedParameterListComponent } from './form-result-oriented-parameter-list.component';

describe('FormResultOrientedParameterListComponent', () => {
  let component: FormResultOrientedParameterListComponent;
  let fixture: ComponentFixture<FormResultOrientedParameterListComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FormResultOrientedParameterListComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FormResultOrientedParameterListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
