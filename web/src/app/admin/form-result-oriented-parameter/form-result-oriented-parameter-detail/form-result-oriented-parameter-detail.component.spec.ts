/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FormResultOrientedParameterDetailComponent } from './form-result-oriented-parameter-detail.component';

describe('FormResultOrientedParameterDetailComponent', () => {
  let component: FormResultOrientedParameterDetailComponent;
  let fixture: ComponentFixture<FormResultOrientedParameterDetailComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FormResultOrientedParameterDetailComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FormResultOrientedParameterDetailComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
