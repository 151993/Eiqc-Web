/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PartMPositionToleranceListComponent } from './part-m-position-tolerance-list.component';

describe('PartMPositionToleranceListComponent', () => {
  let component: PartMPositionToleranceListComponent;
  let fixture: ComponentFixture<PartMPositionToleranceListComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PartMPositionToleranceListComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PartMPositionToleranceListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
