/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FormPackagingListComponent } from './form-packaging-list.component';

describe('FormPackagingListComponent', () => {
  let component: FormPackagingListComponent;
  let fixture: ComponentFixture<FormPackagingListComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FormPackagingListComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FormPackagingListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
