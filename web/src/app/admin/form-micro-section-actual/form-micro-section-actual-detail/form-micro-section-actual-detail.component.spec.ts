/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FormMicroSectionActualDetailComponent } from './form-micro-section-actual-detail.component';

describe('FormMicroSectionActualDetailComponent', () => {
  let component: FormMicroSectionActualDetailComponent;
  let fixture: ComponentFixture<FormMicroSectionActualDetailComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FormMicroSectionActualDetailComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FormMicroSectionActualDetailComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
