/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SupplierFormPartDateCodeListComponent } from './supplier-form-part-date-code-list.component';

describe('SupplierFormPartDateCodeListComponent', () => {
  let component: SupplierFormPartDateCodeListComponent;
  let fixture: ComponentFixture<SupplierFormPartDateCodeListComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SupplierFormPartDateCodeListComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SupplierFormPartDateCodeListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
