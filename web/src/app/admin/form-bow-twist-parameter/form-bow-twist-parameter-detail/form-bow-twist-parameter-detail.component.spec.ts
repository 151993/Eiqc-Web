/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FormBowTwistParameterDetailComponent } from './form-bow-twist-parameter-detail.component';

describe('FormBowTwistParameterDetailComponent', () => {
  let component: FormBowTwistParameterDetailComponent;
  let fixture: ComponentFixture<FormBowTwistParameterDetailComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FormBowTwistParameterDetailComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FormBowTwistParameterDetailComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
