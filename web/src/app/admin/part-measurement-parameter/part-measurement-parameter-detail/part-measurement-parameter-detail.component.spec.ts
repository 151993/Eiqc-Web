/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PartMeasurementParameterDetailComponent } from './part-measurement-parameter-detail.component';

describe('PartMeasurementParameterDetailComponent', () => {
  let component: PartMeasurementParameterDetailComponent;
  let fixture: ComponentFixture<PartMeasurementParameterDetailComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PartMeasurementParameterDetailComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PartMeasurementParameterDetailComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
