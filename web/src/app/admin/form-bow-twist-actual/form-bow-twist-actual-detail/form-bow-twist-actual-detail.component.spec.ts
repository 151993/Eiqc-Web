/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FormBowTwistActualDetailComponent } from './form-bow-twist-actual-detail.component';

describe('FormBowTwistActualDetailComponent', () => {
  let component: FormBowTwistActualDetailComponent;
  let fixture: ComponentFixture<FormBowTwistActualDetailComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FormBowTwistActualDetailComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FormBowTwistActualDetailComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
