/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PartDimensionListComponent } from './part-dimension-list.component';

describe('PartDimensionListComponent', () => {
  let component: PartDimensionListComponent;
  let fixture: ComponentFixture<PartDimensionListComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PartDimensionListComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PartDimensionListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
