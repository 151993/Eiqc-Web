/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LotInspectionQtyDetailComponent } from './lot-inspection-qty-detail.component';

describe('LotInspectionQtyDetailComponent', () => {
  let component: LotInspectionQtyDetailComponent;
  let fixture: ComponentFixture<LotInspectionQtyDetailComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LotInspectionQtyDetailComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LotInspectionQtyDetailComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
