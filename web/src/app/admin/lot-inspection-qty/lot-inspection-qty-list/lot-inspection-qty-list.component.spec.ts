/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LotInspectionQtyListComponent } from './lot-inspection-qty-list.component';

describe('LotInspectionQtyListComponent', () => {
  let component: LotInspectionQtyListComponent;
  let fixture: ComponentFixture<LotInspectionQtyListComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LotInspectionQtyListComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LotInspectionQtyListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
