/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FormMeasurementParameterActualDetailComponent } from './form-measurement-parameter-actual-detail.component';

describe('FormMeasurementParameterActualDetailComponent', () => {
  let component: FormMeasurementParameterActualDetailComponent;
  let fixture: ComponentFixture<FormMeasurementParameterActualDetailComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FormMeasurementParameterActualDetailComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FormMeasurementParameterActualDetailComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
