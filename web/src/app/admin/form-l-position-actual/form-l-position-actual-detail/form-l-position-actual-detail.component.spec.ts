/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FormLPositionActualDetailComponent } from './form-l-position-actual-detail.component';

describe('FormLPositionActualDetailComponent', () => {
  let component: FormLPositionActualDetailComponent;
  let fixture: ComponentFixture<FormLPositionActualDetailComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FormLPositionActualDetailComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FormLPositionActualDetailComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
