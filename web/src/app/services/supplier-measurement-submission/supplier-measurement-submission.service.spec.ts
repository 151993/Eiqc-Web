/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { SupplierMeasurementSubmissionService } from './supplier-measurement-submission.service';

describe('SupplierMeasurementSubmissionService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SupplierMeasurementSubmissionService]
    });
  });

  it('should be created', inject([SupplierMeasurementSubmissionService], (service: SupplierMeasurementSubmissionService) => {
    expect(service).toBeTruthy();
  }));
});
