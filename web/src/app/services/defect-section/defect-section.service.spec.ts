/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';
import { DefectSectionService } from './defect-section.service';


describe('DefectSectionService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DefectSectionService]
    });
  });

  it('should be created', inject([DefectSectionService], (service: DefectSectionService) => {
    expect(service).toBeTruthy();
  }));
});
