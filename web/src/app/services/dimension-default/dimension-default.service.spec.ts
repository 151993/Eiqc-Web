/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { DimensionDefaultService } from './dimension-default.service';

describe('DimensionDefaultService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DimensionDefaultService]
    });
  });

  it('should be created', inject([DimensionDefaultService], (service: DimensionDefaultService) => {
    expect(service).toBeTruthy();
  }));
});
