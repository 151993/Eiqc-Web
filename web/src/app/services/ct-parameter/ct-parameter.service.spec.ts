/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { CTParameterService } from './ct-parameter.service';

describe('CTParameterService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [CTParameterService]
    });
  });

  it('should be created', inject([CTParameterService], (service: CTParameterService) => {
    expect(service).toBeTruthy();
  }));
});
