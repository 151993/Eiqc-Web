/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { PartMeasurementParameterService } from './part-measurement-parameter.service';

describe('PartMeasurementParameterService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PartMeasurementParameterService]
    });
  });

  it('should be created', inject([PartMeasurementParameterService], (service: PartMeasurementParameterService) => {
    expect(service).toBeTruthy();
  }));
});
