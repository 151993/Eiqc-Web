/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { InspectionToolsService } from './inspection-tools.service';

describe('InspectionToolsService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [InspectionToolsService]
    });
  });

  it('should be created', inject([InspectionToolsService], (service: InspectionToolsService) => {
    expect(service).toBeTruthy();
  }));
});
