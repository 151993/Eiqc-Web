/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { FormMPositionService } from './form-m-position.service';

describe('FormMPositionService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [FormMPositionService]
    });
  });

  it('should be created', inject([FormMPositionService], (service: FormMPositionService) => {
    expect(service).toBeTruthy();
  }));
});
