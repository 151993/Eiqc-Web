/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { FormMPositionActualService } from './form-m-position-actual.service';

describe('FormMPositionActualService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [FormMPositionActualService]
    });
  });

  it('should be created', inject([FormMPositionActualService], (service: FormMPositionActualService) => {
    expect(service).toBeTruthy();
  }));
});
