/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { FormPartDateCodeService } from './form-part-date-code.service';

describe('FormPartDateCodeService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [FormPartDateCodeService]
    });
  });

  it('should be created', inject([FormPartDateCodeService], (service: FormPartDateCodeService) => {
    expect(service).toBeTruthy();
  }));
});
