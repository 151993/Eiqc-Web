/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { FormVISService } from './form-vis.service';

describe('FormVISService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [FormVISService]
    });
  });

  it('should be created', inject([FormVISService], (service: FormVISService) => {
    expect(service).toBeTruthy();
  }));
});
