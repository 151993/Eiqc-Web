/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { GRSSAPResultService } from './grssap-result.service';

describe('GRSSAPResultService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [GRSSAPResultService]
    });
  });

  it('should be created', inject([GRSSAPResultService], (service: GRSSAPResultService) => {
    expect(service).toBeTruthy();
  }));
});
