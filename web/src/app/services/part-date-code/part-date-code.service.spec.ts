/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { PartDateCodeService } from './part-date-code.service';

describe('PartDateCodeService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PartDateCodeService]
    });
  });

  it('should be created', inject([PartDateCodeService], (service: PartDateCodeService) => {
    expect(service).toBeTruthy();
  }));
});
