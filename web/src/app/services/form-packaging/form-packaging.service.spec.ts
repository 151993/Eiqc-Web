/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { FormPackagingService } from './form-packaging.service';

describe('FormPackagingService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [FormPackagingService]
    });
  });

  it('should be created', inject([FormPackagingService], (service: FormPackagingService) => {
    expect(service).toBeTruthy();
  }));
});
