/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { PartDimensionService } from './part-dimension.service';

describe('PartDimensionService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PartDimensionService]
    });
  });

  it('should be created', inject([PartDimensionService], (service: PartDimensionService) => {
    expect(service).toBeTruthy();
  }));
});
