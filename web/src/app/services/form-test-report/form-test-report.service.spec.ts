/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { FormTestReportService } from './form-test-report.service';

describe('FormTestReportService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [FormTestReportService]
    });
  });

  it('should be created', inject([FormTestReportService], (service: FormTestReportService) => {
    expect(service).toBeTruthy();
  }));
});
