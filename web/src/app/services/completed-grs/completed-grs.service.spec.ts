/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { CompletedGRSService } from './completed-grs.service';

describe('CompletedGRSService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [CompletedGRSService]
    });
  });

  it('should be created', inject([CompletedGRSService], (service: CompletedGRSService) => {
    expect(service).toBeTruthy();
  }));
});
