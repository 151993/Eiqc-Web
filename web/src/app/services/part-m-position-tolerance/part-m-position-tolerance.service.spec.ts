/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { PartMPositionToleranceService } from './part-m-position-tolerance.service';

describe('PartMPositionToleranceService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PartMPositionToleranceService]
    });
  });

  it('should be created', inject([PartMPositionToleranceService], (service: PartMPositionToleranceService) => {
    expect(service).toBeTruthy();
  }));
});
