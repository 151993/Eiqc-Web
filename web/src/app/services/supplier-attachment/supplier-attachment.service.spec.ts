/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { SupplierAttachmentService } from './supplier-attachment.service';

describe('SupplierAttachmentService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SupplierAttachmentService]
    });
  });

  it('should be created', inject([SupplierAttachmentService], (service: SupplierAttachmentService) => {
    expect(service).toBeTruthy();
  }));
});
