/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { FormResultOrientedParameterService } from './form-result-oriented-parameter.service';

describe('FormResultOrientedParameterService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [FormResultOrientedParameterService]
    });
  });

  it('should be created', inject([FormResultOrientedParameterService], (service: FormResultOrientedParameterService) => {
    expect(service).toBeTruthy();
  }));
});
