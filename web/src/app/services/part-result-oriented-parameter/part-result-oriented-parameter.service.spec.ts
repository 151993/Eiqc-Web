/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { PartResultOrientedParameterService } from './part-result-oriented-parameter.service';

describe('PartResultOrientedParameterService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PartResultOrientedParameterService]
    });
  });

  it('should be created', inject([PartResultOrientedParameterService], (service: PartResultOrientedParameterService) => {
    expect(service).toBeTruthy();
  }));
});
