/* Auto Generated Code By AutoCodeGen Jabil © 2019 */


import { TestBed, inject } from '@angular/core/testing';

import { FormMicroSectionService } from './form-micro-section.service';

describe('FormMicroSectionService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [FormMicroSectionService]
    });
  });

  it('should be created', inject([FormMicroSectionService], (service: FormMicroSectionService) => {
    expect(service).toBeTruthy();
  }));
});
